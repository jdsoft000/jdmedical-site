def draw_clean_logo(page, cx, cy, scale=1.0):
    shape = page.new_shape()
    # Contour
    shape.draw_line(fitz.Point(cx + (-15.0)*scale, cy + (-86.5)*scale), fitz.Point(cx + (-15.0)*scale, cy + (-86.5)*scale))
    last_pt = fitz.Point(cx + (-15.0)*scale, cy + (-86.5)*scale)
    npt = fitz.Point(cx + (-66.0)*scale, cy + (-86.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-66.0)*scale, cy + (51.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-67.0)*scale, cy + (52.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-67.0)*scale, cy + (60.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-68.0)*scale, cy + (61.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-68.0)*scale, cy + (65.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-69.0)*scale, cy + (66.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-69.0)*scale, cy + (69.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-70.0)*scale, cy + (70.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-71.0)*scale, cy + (75.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-73.0)*scale, cy + (78.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-73.0)*scale, cy + (80.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-75.0)*scale, cy + (84.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-77.0)*scale, cy + (86.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-78.0)*scale, cy + (89.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-81.0)*scale, cy + (92.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-81.0)*scale, cy + (93.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-95.0)*scale, cy + (107.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-96.0)*scale, cy + (107.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-98.0)*scale, cy + (109.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-112.0)*scale, cy + (116.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-114.0)*scale, cy + (116.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-115.0)*scale, cy + (117.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-118.0)*scale, cy + (117.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-119.0)*scale, cy + (118.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-123.0)*scale, cy + (118.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-124.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-149.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-150.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-178.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-179.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-192.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-193.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-221.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-222.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-234.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-235.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-253.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-254.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-258.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-259.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-259.0)*scale, cy + (170.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-99.0)*scale, cy + (170.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-98.0)*scale, cy + (169.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-91.0)*scale, cy + (169.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-90.0)*scale, cy + (168.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-86.0)*scale, cy + (168.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-85.0)*scale, cy + (167.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-82.0)*scale, cy + (167.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-81.0)*scale, cy + (166.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-76.0)*scale, cy + (165.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-73.0)*scale, cy + (163.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-71.0)*scale, cy + (163.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-59.0)*scale, cy + (157.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-57.0)*scale, cy + (155.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-53.0)*scale, cy + (153.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-50.0)*scale, cy + (150.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-49.0)*scale, cy + (150.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-35.0)*scale, cy + (136.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-35.0)*scale, cy + (135.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-32.0)*scale, cy + (132.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-29.0)*scale, cy + (126.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-27.0)*scale, cy + (124.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-21.0)*scale, cy + (112.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-21.0)*scale, cy + (110.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-20.0)*scale, cy + (109.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-20.0)*scale, cy + (107.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-18.0)*scale, cy + (103.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-18.0)*scale, cy + (100.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-17.0)*scale, cy + (99.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-17.0)*scale, cy + (95.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-16.0)*scale, cy + (94.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-16.0)*scale, cy + (89.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-15.0)*scale, cy + (88.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    shape.draw_line(last_pt, fitz.Point(cx + (-15.0)*scale, cy + (-86.5)*scale))
    # Contour
    shape.draw_line(fitz.Point(cx + (3.0)*scale, cy + (-175.5)*scale), fitz.Point(cx + (3.0)*scale, cy + (-175.5)*scale))
    last_pt = fitz.Point(cx + (3.0)*scale, cy + (-175.5)*scale)
    npt = fitz.Point(cx + (3.0)*scale, cy + (-125.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (146.0)*scale, cy + (-125.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (147.0)*scale, cy + (-124.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (157.0)*scale, cy + (-123.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (168.0)*scale, cy + (-119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (177.0)*scale, cy + (-114.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (189.0)*scale, cy + (-104.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (199.0)*scale, cy + (-91.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (204.0)*scale, cy + (-81.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (208.0)*scale, cy + (-68.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (209.0)*scale, cy + (-56.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (210.0)*scale, cy + (-55.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (210.0)*scale, cy + (48.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (209.0)*scale, cy + (49.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (209.0)*scale, cy + (57.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (204.0)*scale, cy + (75.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (194.0)*scale, cy + (92.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (180.0)*scale, cy + (106.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (166.0)*scale, cy + (114.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (154.0)*scale, cy + (118.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (141.0)*scale, cy + (119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (140.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-1.0)*scale, cy + (120.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-8.0)*scale, cy + (134.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-22.0)*scale, cy + (152.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-35.0)*scale, cy + (163.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-47.0)*scale, cy + (170.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (151.0)*scale, cy + (170.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (152.0)*scale, cy + (169.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (159.0)*scale, cy + (169.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (160.0)*scale, cy + (168.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (174.0)*scale, cy + (166.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (198.0)*scale, cy + (156.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (218.0)*scale, cy + (142.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (230.0)*scale, cy + (130.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (243.0)*scale, cy + (112.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (250.0)*scale, cy + (98.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (256.0)*scale, cy + (81.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (259.0)*scale, cy + (68.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (259.0)*scale, cy + (61.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (260.0)*scale, cy + (60.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (260.0)*scale, cy + (-67.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (254.0)*scale, cy + (-95.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (245.0)*scale, cy + (-115.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (232.0)*scale, cy + (-134.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (212.0)*scale, cy + (-153.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (187.0)*scale, cy + (-167.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (172.0)*scale, cy + (-172.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (156.0)*scale, cy + (-174.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (155.0)*scale, cy + (-175.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    shape.draw_line(last_pt, fitz.Point(cx + (3.0)*scale, cy + (-175.5)*scale))
    # Contour
    shape.draw_line(fitz.Point(cx + (-15.0)*scale, cy + (-175.5)*scale), fitz.Point(cx + (-15.0)*scale, cy + (-175.5)*scale))
    last_pt = fitz.Point(cx + (-15.0)*scale, cy + (-175.5)*scale)
    npt = fitz.Point(cx + (-171.0)*scale, cy + (-175.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-172.0)*scale, cy + (-174.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-177.0)*scale, cy + (-174.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-178.0)*scale, cy + (-173.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-182.0)*scale, cy + (-173.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-183.0)*scale, cy + (-172.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-185.0)*scale, cy + (-172.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-186.0)*scale, cy + (-171.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-189.0)*scale, cy + (-171.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-195.0)*scale, cy + (-168.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-197.0)*scale, cy + (-168.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-207.0)*scale, cy + (-163.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-209.0)*scale, cy + (-161.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (-160.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-218.0)*scale, cy + (-155.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-219.0)*scale, cy + (-155.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-223.0)*scale, cy + (-151.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-224.0)*scale, cy + (-151.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-238.0)*scale, cy + (-136.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-238.0)*scale, cy + (-135.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-244.0)*scale, cy + (-127.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-245.0)*scale, cy + (-124.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-247.0)*scale, cy + (-122.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-249.0)*scale, cy + (-118.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-249.0)*scale, cy + (-116.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-252.0)*scale, cy + (-111.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-252.0)*scale, cy + (-109.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-253.0)*scale, cy + (-108.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-253.0)*scale, cy + (-106.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-255.0)*scale, cy + (-102.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-255.0)*scale, cy + (-99.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-256.0)*scale, cy + (-98.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-256.0)*scale, cy + (-95.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-257.0)*scale, cy + (-94.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-257.0)*scale, cy + (-89.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-258.0)*scale, cy + (-88.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-258.0)*scale, cy + (-81.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-259.0)*scale, cy + (-80.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-259.0)*scale, cy + (62.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-213.0)*scale, cy + (62.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (61.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-213.0)*scale, cy + (60.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-213.0)*scale, cy + (24.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (23.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (17.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-213.0)*scale, cy + (16.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (14.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (-28.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-213.0)*scale, cy + (-29.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (-30.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-213.0)*scale, cy + (-32.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-213.0)*scale, cy + (-36.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (-37.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-212.0)*scale, cy + (-79.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-211.0)*scale, cy + (-80.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-210.0)*scale, cy + (-87.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-207.0)*scale, cy + (-93.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-207.0)*scale, cy + (-95.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-205.0)*scale, cy + (-97.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-202.0)*scale, cy + (-103.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-195.0)*scale, cy + (-111.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-194.0)*scale, cy + (-111.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-189.0)*scale, cy + (-116.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-186.0)*scale, cy + (-117.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-184.0)*scale, cy + (-119.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-180.0)*scale, cy + (-121.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-178.0)*scale, cy + (-121.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-175.0)*scale, cy + (-123.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-172.0)*scale, cy + (-123.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-171.0)*scale, cy + (-124.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-167.0)*scale, cy + (-124.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-166.0)*scale, cy + (-125.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    npt = fitz.Point(cx + (-15.0)*scale, cy + (-125.5)*scale)
    shape.draw_line(last_pt, npt)
    last_pt = npt
    shape.draw_line(last_pt, fitz.Point(cx + (-15.0)*scale, cy + (-175.5)*scale))
